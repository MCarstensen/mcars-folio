import Container from '../Container';
import tw from 'twin.macro';

const Footer: React.FC = () => {
  return (
    <footer tw="bg-gray-800 border-t border-white">
      <Container>
        <span>footer</span>
      </Container>
    </footer>
  );
};

export default Footer;
